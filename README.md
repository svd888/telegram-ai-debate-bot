# 🤖 Telegram AI Debate Bot

Telegram-бот с интеграцией нескольких мощных AI моделей через OpenRouter и уникальным режимом дебатов для получения наиболее точных и обоснованных ответов.

## 🌟 Особенности

- **4 мощнейших AI модели**: Google Gemini 3 Pro, Claude Opus 4.5, Grok 4.1, ChatGPT 5.1 Reasoning High
- **Режим дебатов**: Модели обсуждают вопрос в несколько раундов, повышая точность ответа
- **3 режима работы**: Быстрый (2 раунда), Стандартный (3 раунда), Глубокий (5 раундов)
- **Асинхронная архитектура**: Быстрая обработка запросов
- **Подробное логирование**: Отслеживание всех операций
- **Сохранение истории**: Все дебаты сохраняются в JSON формате

## 📋 Требования

- Python 3.11+
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))
- OpenRouter API Key (получить на [OpenRouter](https://openrouter.ai/keys))

## 🚀 Установка

### 1. Клонирование репозитория

```bash
cd telegram-ai-debate-bot
```

### 2. Создание виртуального окружения

```bash
python3.11 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните необходимые данные:

```bash
cp .env.example .env
nano .env  # или используйте любой текстовый редактор
```

Заполните следующие переменные:

```env
# Telegram Bot Token (получить у @BotFather)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# OpenRouter API Key (получить на https://openrouter.ai/keys)
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Настройки дебатов
DEFAULT_DEBATE_ROUNDS=3
DEFAULT_TEMPERATURE=0.2

# Логирование
LOG_LEVEL=INFO
```

### 5. Получение Telegram Bot Token

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям: укажите имя и username бота
4. Скопируйте полученный токен в `.env` файл

### 6. Получение OpenRouter API Key

1. Зарегистрируйтесь на [OpenRouter](https://openrouter.ai/)
2. Перейдите в раздел [API Keys](https://openrouter.ai/keys)
3. Создайте новый ключ
4. Скопируйте ключ в `.env` файл
5. **Важно**: Пополните баланс на OpenRouter для доступа к моделям

## ▶️ Запуск

```bash
cd src
python main.py
```

Бот запустится и начнет принимать сообщения в Telegram.

## 📱 Использование

### Команды бота

- `/start` - Начать работу с ботом
- `/ask` - Задать вопрос одной AI модели
- `/debate` - Запустить режим дебатов между моделями
- `/settings` - Настроить параметры работы
- `/history` - Посмотреть историю запросов
- `/help` - Показать справку

### Режимы дебатов

#### ⚡ Быстрый (2 раунда)
- **Время**: ~30-60 секунд
- **Использование**: Простые вопросы, быстрые ответы
- **Раунды**: 
  1. Независимые ответы всех моделей
  2. Краткое обсуждение и синтез

#### 📊 Стандартный (3 раунда) - рекомендуется
- **Время**: ~60-90 секунд
- **Использование**: Большинство вопросов
- **Раунды**:
  1. Независимые ответы
  2. Обсуждение и аргументация
  3. Финальный синтез

#### 🔬 Глубокий (5 раундов)
- **Время**: ~120-180 секунд
- **Использование**: Сложные вопросы, требующие глубокого анализа
- **Раунды**:
  1. Независимые ответы
  2-4. Многоуровневое обсуждение
  5. Финальный синтез

### Как работает режим дебатов

1. **Раунд 1**: Все 4 модели получают вопрос и генерируют независимые ответы
2. **Раунды 2-N**: Каждая модель видит ответы других и может:
   - Согласиться с сильными аргументами
   - Предложить контраргументы
   - Уточнить свою позицию
   - Указать степень уверенности
3. **Синтез**: ChatGPT 5.1 анализирует все раунды и создает финальный ответ с указанием уверенности

## 🏗️ Архитектура проекта

```
telegram-ai-debate-bot/
├── src/
│   ├── bot/
│   │   ├── handlers.py          # Обработчики команд
│   │   └── keyboards.py         # Клавиатуры
│   ├── ai/
│   │   ├── openrouter_client.py # Клиент OpenRouter API
│   │   ├── debate_manager.py    # Логика дебатов
│   │   └── models.py            # Модели данных
│   ├── utils/
│   │   ├── config.py            # Конфигурация
│   │   └── logger.py            # Логирование
│   └── main.py                  # Точка входа
├── data/
│   └── debates/                 # Сохраненные дебаты
├── logs/                        # Логи работы
├── .env                         # Переменные окружения
├── config.yaml                  # Конфигурация моделей
├── requirements.txt             # Зависимости
└── README.md                    # Документация
```

## 🔧 Конфигурация

### config.yaml

В файле `config.yaml` можно настроить:

- **Модели**: ID моделей в OpenRouter, параметры генерации
- **Режимы дебатов**: Количество раундов для каждого режима
- **Системные промпты**: Инструкции для моделей на каждом этапе
- **OpenRouter настройки**: Таймауты, повторные попытки

### Используемые модели

| Модель | ID в OpenRouter | Описание |
|--------|----------------|----------|
| 🔵 Gemini 3 Pro | `google/gemini-3-pro` | Мощная модель Google |
| 🟣 Claude Opus 4.5 | `anthropic/claude-opus-4.5` | Продвинутая модель Anthropic |
| 🟢 Grok 4.1 | `x-ai/grok-4.1` | Модель xAI с глубоким рассуждением |
| 🟠 ChatGPT 5.1 | `openai/gpt-5.1-reasoning-high` | OpenAI с усиленным режимом рассуждений |

**⚠️ Важно**: Точные ID моделей могут отличаться. Проверьте актуальные названия на [OpenRouter Models](https://openrouter.ai/models).

## 💡 Примеры использования

### Пример 1: Простой вопрос к одной модели

1. Отправьте `/ask`
2. Выберите модель (например, ChatGPT 5.1)
3. Отправьте вопрос: "Что такое квантовая запутанность?"
4. Получите ответ с указанием уверенности

### Пример 2: Дебаты по сложному вопросу

1. Отправьте `/debate`
2. Выберите режим "Глубокий"
3. Отправьте вопрос: "Какие основные причины инфляции в 2024 году?"
4. Дождитесь завершения 5 раундов дебатов
5. Получите синтезированный ответ с анализом всех точек зрения

## 📊 Логирование

Логи сохраняются в директории `logs/`:

- `bot.log` - Общие логи работы (ротация 10 МБ, хранение 7 дней)
- `errors.log` - Только ошибки (ротация 10 МБ, хранение 30 дней)

Уровень логирования настраивается в `.env` через `LOG_LEVEL`.

## 💾 Сохранение дебатов

Все дебаты автоматически сохраняются в `data/debates/` в формате JSON:

```json
{
  "session_id": "uuid",
  "user_id": 123456,
  "question": "Вопрос пользователя",
  "mode": "standard",
  "rounds": [...],
  "final_answer": "Финальный ответ",
  "final_confidence": 92.5,
  "total_tokens": 15234
}
```

## ⚠️ Важные замечания

### Проверка ID моделей в OpenRouter

**Критически важно**: ID моделей в `config.yaml` могут не соответствовать реальным ID в OpenRouter API. Перед запуском необходимо:

1. Посетить [OpenRouter Models](https://openrouter.ai/models)
2. Найти актуальные ID для моделей:
   - Gemini 3 Pro
   - Claude Opus 4.5
   - Grok 4.1
   - ChatGPT 5.1 Reasoning High
3. Обновить ID в `config.yaml`

**Пример проверки через API**:

```python
import requests

headers = {
    'Authorization': f'Bearer YOUR_OPENROUTER_API_KEY'
}

response = requests.get('https://openrouter.ai/api/v1/models', headers=headers)
models = response.json()

# Найдите нужные модели в списке
for model in models['data']:
    print(f"{model['id']}: {model['name']}")
```

### Стоимость использования

- OpenRouter взимает плату за использование моделей
- Режим дебатов использует несколько моделей одновременно
- Глубокий режим (5 раундов) потребляет значительно больше токенов
- Рекомендуется установить лимиты расходов в настройках OpenRouter

### Ограничения

- Максимальная длина сообщения Telegram: 4096 символов
- Длинные ответы автоматически разбиваются на части
- Таймаут запроса к OpenRouter: 60 секунд
- Количество повторных попыток при ошибке: 3

## 🐛 Устранение неполадок

### Бот не запускается

1. Проверьте правильность `TELEGRAM_BOT_TOKEN` в `.env`
2. Убедитесь, что установлены все зависимости: `pip install -r requirements.txt`
3. Проверьте логи в `logs/errors.log`

### Ошибки при запросах к моделям

1. Проверьте баланс на OpenRouter
2. Убедитесь, что `OPENROUTER_API_KEY` корректен
3. Проверьте ID моделей в `config.yaml`
4. Посмотрите логи для деталей ошибки

### Медленная работа дебатов

- Это нормально для глубокого режима (5 раундов)
- Используйте быстрый режим для простых вопросов
- Проверьте скорость интернет-соединения

## 📈 Дальнейшее развитие

Планируемые функции:

- [ ] База данных для хранения истории
- [ ] Веб-интерфейс для просмотра дебатов
- [ ] Поддержка изображений в вопросах
- [ ] Статистика использования моделей
- [ ] Экспорт дебатов в PDF
- [ ] Пользовательские настройки моделей
- [ ] Мультиязычная поддержка

## 📄 Лицензия

MIT License

## 👨‍💻 Автор

Создано для демонстрации возможностей интеграции множественных AI моделей через OpenRouter.

## 🤝 Поддержка

При возникновении вопросов или проблем:

1. Проверьте раздел "Устранение неполадок"
2. Изучите логи в `logs/`
3. Создайте issue в репозитории

---

**Уверенность в документации: 95%**

Документация основана на реальной архитектуре проекта. Единственная неопределенность - точные ID моделей в OpenRouter, которые необходимо проверить перед запуском.
